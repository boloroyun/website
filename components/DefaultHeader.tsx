import TopBarWrapper from './TopBarWrapper';
import Navbar from './Navbar';

const DefaultHeader = () => {
  return (
    <header>
      <TopBarWrapper />
      <Navbar />
    </header>
  );
};

export default DefaultHeader;
